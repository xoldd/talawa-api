{
	"if current user is not authenticated": "throw unauthenticated error",
	"if current user is authenticated": {
		"if current user does not exist": "throw unauthenticated error",
		"if current user exists": {
			"if current user is a regular user": {
				"if current user is not the created user": "throw unauthorized error",
				"if current user is the created user": {
					"if current user id is not equal to the creator user id": {
						"if creator user does not exist": "return unexpected error",
						"if creator user exists": "return the creator user"
					},
					"if current user id is equal to the creator user id": "return current user as the creator user"
				}
			},
			"if current user is an administrator user": {
				"if current user id is not equal to the creator user id": {
					"if creator user does not exist": "return unexpected error",
					"if creator user exists": "return the creator user"
				},
				"if current user id is equal to the creator user idr": "return current user as the creator user"
			}
		}
	}
}
