{
	"if current user is not authenticated": "throw unauthenticated error",
	"if current user is authenticated": {
		"if zod arguments parsing does not succeed": "throw invalid arguments error",
		"if zod arguments parsing succeeds": {
			"if graphql connection cursor is not provided by the client": "return the graphql connection",
			"if graphql connection cursor is provided by the client": {
				"if it is invalid": "throw not found error",
				"if it is valid": "return the graphql connection"
			}
		}
	}
}
